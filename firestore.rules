rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**}/NSW_sites/{site} {
      allow read: if request.auth != null;
    }
    
    match /{document=**}/VIC_sites/{site} {
      allow read: if request.auth != null;
    }
  }
}